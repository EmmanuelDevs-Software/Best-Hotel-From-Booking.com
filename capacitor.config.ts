import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Best-booking',
  webDir: 'dist/best-booking',
  bundledWebRuntime: false
};

export default config;
